if currentUser==='manager'
  include dash-manager
else if currentUser==='agent'
  include dash-agent
else 
  include noneuser.pug
script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js')
